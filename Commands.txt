Command for Dashboards running:

Jenkins:


K8s Dashboars:

http://localhost:8081/

kubectl port-forward -n kubernetes-dashboard service/kubernetes-dashboard 8081:8081

ArgoCD Dashboards:

http://localhost:9090

kubectl port-forward svc/argocd-server -n argocd 9090:443

Grafana Dashboards:

http://localhost:3000/

 export POD_NAME=$(kubectl get pods --namespace lanabot-ns -l "app.kubernetes.io/name=grafana,app.kubernetes.io/instance=lanabot-grafana" -o jsonpath="{.items[0].metadata.name}")
     kubectl --namespace lanabot-ns port-forward $POD_NAME 3000
