# k8s/service-demo.yaml

apiVersion: v1
kind: Service
metadata:
  name: lanabot-service
  labels:
    app: lana-bot
    project: lanabot-project
    env: dev
    release: v1.0
spec:
  selector:
    app: lana-bot
  ports:
    - protocol: TCP
      port: 8443
      targetPort: 8443